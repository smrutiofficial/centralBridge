import 'package:flutter/material.dart';

class SmsHistoryPage extends StatelessWidget {
  const SmsHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SMS History Page"),
    );
  }
}