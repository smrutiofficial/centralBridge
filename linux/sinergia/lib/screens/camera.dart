import 'package:flutter/material.dart';

class CameraCastPage extends StatelessWidget {
  const CameraCastPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Camera Cast Page"),
    );
  }
}