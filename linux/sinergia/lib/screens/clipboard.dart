import 'package:flutter/material.dart';

class ClipboardSyncPage extends StatelessWidget {
  const ClipboardSyncPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Clipboard Sync Page"),
    );
  }
}