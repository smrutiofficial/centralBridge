import 'package:flutter/material.dart';

class PhoneSyncPage extends StatelessWidget {
  const PhoneSyncPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Phone Sync Page"),
    );
  }
}