import 'package:flutter/material.dart';

class ScreenCastPage extends StatelessWidget {
  const ScreenCastPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Screen Cast Page"),
    );
  }
}